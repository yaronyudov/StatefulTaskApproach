events {}
http {
  server {
    listen 9099;
    # Serve the crafted provider feed as JSON at /events (the path the scrapper polls).
    location = /events {
      default_type application/json;
      alias /data/events.json;
    }
  }
}
