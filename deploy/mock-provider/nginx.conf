events {}
http {
  server {
    listen 9099;
    # Serve the crafted provider feed as JSON at /events (the path the scraper polls).
    location = /events {
      default_type application/json;
      alias /data/events.json;
    }
  }
}
